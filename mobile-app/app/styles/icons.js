const tinyLogo = {
    width: 20,
    height: 20,
    resizeMode: 'contain',
    marginRight: 10
}

const tinyIcon = {
    padding: 10
}

const tinierLogo = {
    width: 18,
    height: 18,
    resizeMode: 'contain',
    marginLeft: 20
}

const tiniestLogo = {
    width: 15,
    height: 15,
    resizeMode: 'contain',
    marginLeft: 20
}

const bigImage = {
    width: 200,
    height: 200,
    resizeMode: 'contain',
}

// main
const iconStyles = {
    tinyLogo : tinyLogo,
    tinyIcon : tinyIcon,
    tinierLogo: tinierLogo,
    tiniestLogo: tiniestLogo,
    bigImage: bigImage,
}

export default iconStyles;